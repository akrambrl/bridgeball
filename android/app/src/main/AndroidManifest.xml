<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>

        <!-- ADMOB — L'IDENTIFIANT D'APPLICATION EST OBLIGATOIRE.
             Sans cette balise, l'app NE DÉMARRE PAS : le SDK Google Mobile Ads
             lève une exception au lancement et Android ferme le processus. Ce
             n'est pas un avertissement, c'est un crash immédiat, et il ne se voit
             qu'une fois l'AAB installé sur un téléphone.

             Celui-ci est l'identifiant de TEST de Google, public et documenté. Il
             laisse l'app démarrer et sert des pubs d'essai. À remplacer par
             l'identifiant d'application de ton compte AdMob — celui en ~, pas
             celui de l'unité publicitaire en / (ce sont deux choses différentes,
             et les confondre donne le même crash).
             https://developers.google.com/admob/android/quick-start -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713" />
        
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
